let fruits = ["Banana", "Orange", "Apple", "Mango", "Melon"];
// fruits.toString()
// console.log(fruits[2].split("", 3));




let word1 = " Freelance with the best of the best Silicon Valley companies in a number of software jobs  ";

// padding is divided into two padStart and padEnd which support padding at the beginning and at the end of a string

// The padStart() method pads the current string with another string (multiple times, if needed) until the resulting string reaches the given length. The padding is applied from the start of the current string. 

let word2 = "i am more than able";

// let result = word2.padStart(34,"0");
let result = word2.padEnd(22, " mw");
console.log(result);
 
